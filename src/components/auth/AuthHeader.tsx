const AuthHeader = () => {
    return (
        <div className="auth-header">
            <h2 className="heading-xl gradient-text auth-title">Focus Flow</h2>
            <p className="auth-subtitle">Initiate Session Protocol</p>
        </div>
    );
};

export default AuthHeader;
